source "https://rubygems.org"

# Explicitly override minima so Jekyll doesn't load it by default
gem "jekyll", "~> 4.3"
gem "jekyll-seo-tag"

# Uncomment below when deploying to GitHub Pages:
# gem "github-pages", group: :jekyll_plugins
